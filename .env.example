DATABASE_URL="postgresql://postgres:admin123@localhost:5432/correas_center?schema=public"
SEED_ADMIN_EMAIL="admin@correascenter.com"
SEED_ADMIN_PASSWORD=""
API_PORT="3001"
# Generar un secreto JWT de 32 bytes en base64url para el entorno local con el siguiente comando:
# node --input-type=module -e "import { randomBytes } from 'node:crypto'; console.log(randomBytes(32).toString('base64url'))"
IAM_JWT_SECRET_B64=""
IAM_JWT_ISSUER="correas-center-local"
IAM_JWT_AUDIENCE="correas-center-api"
PORTAL_ORIGIN="http://localhost:5173"